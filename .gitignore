.env
backend/keys/
backend/.env
frontend/.env

# Build do frontend — gerado pelo Railway no deploy, nunca commitar
# (evita vazar VITE_FIREBASE_* keys no repositório público)
backend/public/spa/
node_modules

# Arquivos temporários do OneDrive
.tmp.driveupload/

# Worktrees do Claude Code (sessões de agente)
.claude/worktrees/

# Arquivos temporários sem nome
Untitled
